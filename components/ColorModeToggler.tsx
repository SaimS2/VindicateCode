import React from 'react';

// This component is disabled and returns null to remove it from the UI
// and disable the grayscale filter functionality entirely.
const ColorModeToggler: React.FC = () => {
    return null;
};

export default ColorModeToggler;
